<Window x:Class="Pie.Views.GroupEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Group"
        Icon="/Pie.ico"
        Height="500" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F7"
        ResizeMode="CanResize"
        MinWidth="350" MinHeight="400">

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007AFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0066DD"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0055CC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#E8E8ED"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D8D8DD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Group Name" FontSize="14" FontWeight="Medium" Margin="0,0,0,4"/>
        <TextBox x:Name="GroupNameTextBox" Grid.Row="1" Padding="10,8" FontSize="13" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="2" Text="Applications in Group" FontSize="14" FontWeight="Medium" Margin="0,0,0,8"/>

        <ListBox x:Name="AppsListBox" Grid.Row="3" Background="White" BorderBrush="#DDD" BorderThickness="1">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="4">
                        <TextBlock Text="{Binding Name}" FontSize="13" VerticalAlignment="Center"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,12,0,0">
            <Button x:Name="AddAppButton" Content="Add App" Style="{StaticResource SecondaryButtonStyle}" Click="AddAppButton_Click"/>
            <Button x:Name="BrowseInstalledButton" Content="Browse Installed" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0" Click="BrowseInstalledButton_Click"/>
            <Button x:Name="RemoveButton" Content="Remove" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0" Click="RemoveButton_Click"/>
            <Button x:Name="MoveUpButton" Content="Up" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0" Click="MoveUpButton_Click"/>
            <Button x:Name="MoveDownButton" Content="Down" Style="{StaticResource SecondaryButtonStyle}" Margin="8,0,0,0" Click="MoveDownButton_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CancelButton" Content="Cancel" Style="{StaticResource SecondaryButtonStyle}" Click="CancelButton_Click" Margin="0,0,8,0"/>
            <Button x:Name="SaveButton" Content="Save" Style="{StaticResource ModernButtonStyle}" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
